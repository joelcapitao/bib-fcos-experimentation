apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: fcos-metal-push
  annotations:
    build.appstudio.openshift.io/repo: https://github.com/joelcapitao/bib-fcos-experimentation?rev={{revision}}
    build.appstudio.redhat.com/commit_sha: '{{revision}}'
    build.appstudio.redhat.com/target_branch: '{{target_branch}}'
    pipelinesascode.tekton.dev/on-cel-expression: |
      event == "push" && target_branch == "main"
    pipelinesascode.tekton.dev/cancel-in-progress: "false"
    pipelinesascode.tekton.dev/max-keep-runs: "3"
  labels:
    appstudio.openshift.io/application: fcos-disk-images
    # TODO: derive the branch prefix ("rawhide") dynamically instead of hardcoding
    appstudio.openshift.io/component: rawhide-x86-metal
    pipelines.appstudio.openshift.io/type: build
spec:
  params:
    - name: git-url
      value: '{{source_url}}'
    - name: revision
      value: '{{revision}}'
    - name: output-image
      value: quay.io/redhat-user-workloads/fcos-tenant/fcos-metal:{{revision}}
    - name: image-expires-after
      value: ""
    - name: build-platforms
      value:
        - linux/x86_64
        - linux/aarch64
        - linux/ppc64le
        - linux/s390x
    - name: artifact
      value: metal
    - name: source-image
      value: quay.io/fedora/fedora-coreos:rawhide
  pipelineRef:
    name: fcos-disk-image
  taskRunTemplate:
    serviceAccountName: build-pipeline-fcos-metal
  workspaces:
    - name: git-auth
      secret:
        secretName: '{{git_auth_secret}}'
