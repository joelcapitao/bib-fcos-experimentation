# Platform: metal — ppc64le
# No console kernel arguments for metal; the installer sets those at install
# time via coreos-installer --console.
[customizations.kernel]
append = "ignition.platform.id=metal"
